import React from 'react'

const ThemeEditorPanel = () => {
  return (
    <div style= {styles.panelContainer}>
      This is a editor panel
    </div>
  )
}

export default ThemeEditorPanel

const styles = {
  panelContainer: {
    minWidth: 200,
  },
}